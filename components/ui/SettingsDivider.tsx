import React from 'react';
import { View } from 'react-native';
import { theme } from '@/lib/theme';

const SettingsDivider: React.FC = () => <View style={theme.settingsStyles.divider} />;

export default SettingsDivider;
